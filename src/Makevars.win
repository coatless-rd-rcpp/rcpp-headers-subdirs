# Default make file
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

# Added commands
SOURCES=$(wildcard A/*.cpp B/*.cpp)

OBJECTS = main.o $(SOURCES:.cpp=.o)

PKG_CPPFLAGS=-I.

# PKG_CXXFLAGS=-fno-omit-frame-pointer

all: $(SHLIB)

#$(OBJECTS): clean

clean:
	@rm -f $(OBJECTS)
