# Default make file
PKG_CPPFLAGS=-I.
PKG_LIBS=$(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

# Added commands
SOURCES=$(wildcard A/*.cpp B/*.cpp)

OBJECTS=main.o $(SOURCES:.cpp=.o)

all: $(SHLIB)

clean:
	@rm -f $(OBJECTS)
